import {LayoutProps} from "@/types/LayoutProps";

const ServiceTopLeftSideContentLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div>
            {children}
        </div>
    )
}

export default ServiceTopLeftSideContentLayout;